extends layout
block content
	if(Jobcards[0].jobcardlocks != undefined)
		form(id="msform", method="post", style="color:#12375c;")
			h3(class="fs-title" style="margin-top:20px; margin-bottom:20px; border-bottom: 5px solid #a1c861 !important;") <span data-i18n="demo.preventative_maint.locks"></span>
			.row(style="margin-bottom:18px;")
				h3(class="fs-title" style="margin-bottom:15px;") <span data-i18n="demo.preventative_maint.locksgate"></span>
				.col.s3.m3(style="margin-left:37px;")
					-if(Jobcards[0].jobcardlocks.jobcard_locksgate=="ok")
						input.bom(type="radio" id="jobcard_locksgateok" name="jobcard_locksgate"  value="ok" checked)
					-else
						input.bom(type="radio" id="jobcard_locksgateok" name="jobcard_locksgate"  value="ok")
					label(for="jobcard_locksgateok") Ok	
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksgate=="not ok")
						input.mau(type="radio", id="jobcard_locksgatenotok", name='jobcard_locksgate', value="not ok" checked)
					-else
						input.mau(type="radio", id="jobcard_locksgatenotok", name='jobcard_locksgate', value="not ok")
					label(for="jobcard_locksgatenotok") Not Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksgate=="na")
						input.moderado(type="radio", id="jobcard_locksgatena", name='jobcard_locksgate', value="na" checked)
					-else
						input.moderado(type="radio", id="jobcard_locksgatena", name='jobcard_locksgate', value="na")
					label(for="jobcard_locksgatena") N/A
			.row(style="margin-bottom:18px;")
				h3(class="fs-title" style="margin-bottom:15px;") <span data-i18n="demo.preventative_maint.locksP3"></span>
				.col.s3.m3(style="margin-left:37px;")
					-if(Jobcards[0].jobcardlocks.jobcard_locksP3=="ok")
						input.bom(type="radio" id="jobcard_locksP3ok" name="jobcard_locksP3"  value="ok" checked)
					-else
						input.bom(type="radio" id="jobcard_locksP3ok" name="jobcard_locksP3"  value="ok")
					label(for="jobcard_locksP3ok") Ok	
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksP3=="not ok")
						input.mau(type="radio", id="jobcard_locksP3notok", name='jobcard_locksP3', value="not ok" checked)
					-else
						input.mau(type="radio", id="jobcard_locksP3notok", name='jobcard_locksP3', value="not ok")
					label(for="jobcard_locksP3notok") Not Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksP3=="na")
						input.moderado(type="radio", id="jobcard_locksP3na", name='jobcard_locksP3', value="na" checked)
					-else
						input.moderado(type="radio", id="jobcard_locksP3na", name='jobcard_locksP3', value="na")
					label(for="jobcard_locksP3na") N/A
			.row(style="margin-bottom:18px;")
				h3(class="fs-title" style="margin-bottom:15px;") <span data-i18n="demo.preventative_maint.locksgenset"></span>
				.col.s3.m3(style="margin-left:37px;")
					-if(Jobcards[0].jobcardlocks.jobcard_locksgenset=="ok")
						input.bom(type="radio" id="jobcard_locksgensetok" name="jobcard_locksgenset"  value="ok" checked)
					-else
						input.bom(type="radio" id="jobcard_locksgensetok" name="jobcard_locksgenset"  value="ok")
					label(for="jobcard_locksgensetok") Ok	
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksgenset=="not ok")
						input.mau(type="radio", id="jobcard_locksgensetnotok", name='jobcard_locksgenset', value="not ok" checked)
					-else
						input.mau(type="radio", id="jobcard_locksgensetnotok", name='jobcard_locksgenset', value="not ok")
					label(for="jobcard_locksgensetnotok") Not Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksgenset=="na")
						input.moderado(type="radio", id="jobcard_locksgensetna", name='jobcard_locksgenset', value="na" checked)
					-else
						input.moderado(type="radio", id="jobcard_locksgensetna", name='jobcard_locksgenset', value="na")
					label(for="jobcard_locksgensetna") N/A
			.row(style="margin-bottom:18px;")
				h3(class="fs-title" style="margin-bottom:15px;") <span data-i18n="demo.preventative_maint.lockscontainer"></span>
				.col.s3.m3(style="margin-left:37px;")
					-if(Jobcards[0].jobcardlocks.jobcard_lockscontainer=="ok")
						input.bom(type="radio" id="jobcard_lockscontainerok" name="jobcard_lockscontainer"  value="ok" checked)
					-else
						input.bom(type="radio" id="jobcard_lockscontainerok" name="jobcard_lockscontainer"  value="ok")
					label(for="jobcard_lockscontainerok") Ok	
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_lockscontainer=="not ok")
						input.mau(type="radio", id="jobcard_lockscontainernotok", name='jobcard_lockscontainer', value="not ok" checked)
					-else
						input.mau(type="radio", id="jobcard_lockscontainernotok", name='jobcard_lockscontainer', value="not ok")
					label(for="jobcard_lockscontainernotok") Not Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_lockscontainer=="na")
						input.moderado(type="radio", id="jobcard_lockscontainerna", name='jobcard_lockscontainer', value="na" checked)
					-else
						input.moderado(type="radio", id="jobcard_lockscontainerna", name='jobcard_lockscontainer', value="na")
					label(for="jobcard_lockscontainerna") N/A
			.row(style="margin-bottom:18px;")
				h3(class="fs-title" style="margin-bottom:15px;") <span data-i18n="demo.preventative_maint.locksM3"></span>
				.col.s3.m3(style="margin-left:37px;")
					-if(Jobcards[0].jobcardlocks.jobcard_locksM3=="ok")
						input.bom(type="radio" id="jobcard_locksM3ok" name="jobcard_locksM3"  value="ok" checked)
					-else
						input.bom(type="radio" id="jobcard_locksM3ok" name="jobcard_locksM3"  value="ok")
					label(for="jobcard_locksM3ok") Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksM3=="not ok")
						input.mau(type="radio", id="jobcard_locksM3notok", name='jobcard_locksM3', value="not ok" checked)
					-else
						input.mau(type="radio", id="jobcard_locksM3notok", name='jobcard_locksM3', value="not ok")
					label(for="jobcard_locksM3notok") Not Ok
				.col.s3.m3
					-if(Jobcards[0].jobcardlocks.jobcard_locksM3=="na")
						input.moderado(type="radio", id="jobcard_locksM3na", name='jobcard_locksM3', value="na" checked)
					-else
						input.moderado(type="radio", id="jobcard_locksM3na", name='jobcard_locksM3', value="na")
					label(for="jobcard_locksM3na") N/A
			.row(style="margin-bottom:18px;")
				.col.s12.m12.input-field
					label(for="jobcard_lockscomments") <span data-i18n="demo.preventative_maint.comentarios"></span>
					input( autocomplete="off", type="text", style="font-family:;color:#12375c" ,id="jobcard_lockscomments" ,name="jobcard_lockscomments" value="#{Jobcards[0].jobcardlocks.jobcard_lockscomments}")
			a(class="waves-effect waves-light action-button" style="width:100px; font-size:15px" href="/manutencao/detalhesJobcardPlanned/#{Jobcards[0]._id}") << Back
	else
		h3(class="fs-title selmecAzul center" style="margin-top:20px; margin-bottom:22px; border-bottom: 5px solid #a1c861 !important;") <span data-i18n="demo.preventative_maint.locks"></span>
		form#formulario2(style="margin:0px; color:#12375c;" dataref="#{Jobcards[0]._id}")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.locksgate"></span>
				.row.center(style="margin-bottom:20px")
					.col.s5.m3
						input.bom(type="radio" id="jobcard_locksgateok" name="jobcard_locksgate"  value="ok")
						label(for="jobcard_locksgateok") Ok					
					.col.s5.m3
						input.mau(type="radio", id="jobcard_locksgatenotok", name='jobcard_locksgate', value="not ok")
						label(for="jobcard_locksgatenotok") Not Ok
					.col.s5.m3
						input.moderado(type="radio", id="jobcard_locksgatena", name='jobcard_locksgate', value="na")
						label(for="jobcard_locksgatena") N/A
				input(type="button", name="next", class="next action-button", value="Next >>")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.locksP3"></span>
				.row.center(style="margin-bottom:20px")
					.col.s5.m3
						input.bom(type="radio" id="jobcard_locksP3ok" name="jobcard_locksP3"  value="ok")
						label(for="jobcard_locksP3ok") Ok					
					.col.s5.m3
						input.mau(type="radio", id="jobcard_locksP3notok", name='jobcard_locksP3', value="not ok")
						label(for="jobcard_locksP3notok") Not Ok
					.col.s5.m3
						input.moderado(type="radio", id="jobcard_locksP3na", name='jobcard_locksP3', value="na")
						label(for="jobcard_locksP3na") N/A
				input(type="button", name="previous", class="previous action-button", value="<< Prev")
				input(type="button", name="next", class="next action-button", value="Next >>")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.locksgenset"></span>
				.row.center(style="margin-bottom:20px")
					.col.s5.m3
						input.bom(type="radio" id="jobcard_locksgensetok" name="jobcard_locksgenset"  value="ok")
						label(for="jobcard_locksgensetok") Ok					
					.col.s5.m3
						input.mau(type="radio", id="jobcard_locksgensetnotok", name='jobcard_locksgenset', value="not ok")
						label(for="jobcard_locksgensetnotok") Not Ok
					.col.s5.m3
						input.moderado(type="radio", id="jobcard_locksgensetna", name='jobcard_locksgenset', value="na")
						label(for="jobcard_locksgensetna") N/A
				input(type="button", name="previous", class="previous action-button", value="<< Prev")
				input(type="button", name="next", class="next action-button", value="Next >>")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.lockscontainer"></span>
				.row.center(style="margin-bottom:20px")
					.col.s5.m3
						input.bom(type="radio" id="jobcard_lockscontainerok" name="jobcard_lockscontainer"  value="ok")
						label(for="jobcard_lockscontainerok") Ok					
					.col.s5.m3
						input.mau(type="radio", id="jobcard_lockscontainernotok", name='jobcard_lockscontainer', value="not ok")
						label(for="jobcard_lockscontainernotok") Not Ok
					.col.s5.m3
						input.moderado(type="radio", id="jobcard_lockscontainerna", name='jobcard_lockscontainer', value="na")
						label(for="jobcard_lockscontainerna") N/A
				input(type="button", name="previous", class="previous action-button", value="<< Prev")
				input(type="button", name="next", class="next action-button", value="Next >>")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.locksM3"></span>
				.row.center(style="margin-bottom:20px")
					.col.s5.m3
						input.bom(type="radio" id="jobcard_locksM3ok" name="jobcard_locksM3"  value="ok")
						label(for="jobcard_locksM3ok") Ok					
					.col.s5.m3
						input.mau(type="radio", id="jobcard_locksM3notok", name='jobcard_locksM3', value="not ok")
						label(for="jobcard_locksM3notok") Not Ok
					.col.s5.m3
						input.moderado(type="radio", id="jobcard_locksM3na", name='jobcard_locksM3', value="na")
						label(for="jobcard_locksM3na") N/A
				input(type="button", name="previous", class="previous action-button", value="<< Prev")
				input(type="button", name="next", class="next action-button", value="Next >>")
			fieldset(style="padding:0px")
				h3(class="fs-title") <span data-i18n="demo.preventative_maint.comentarios"></span>
				.row(style="margin-bottom:20px")
					.col.s12.m12.input-field
						input( autocomplete="off", type="text", style="font-family:;color:#12375c" ,id="jobcard_lockscomments" ,name="jobcard_lockscomments")
				input(type="button", name="previous", class="previous action-button", value="<< Prev")
				input(type="button", id="jobcardlocks_yes_btn", class="action-button" value="Save")
	block js
		script(type="text/javascript" src="/js/jquery.mask.js")
		script.

			
			//jQuery time
			var current_fs, next_fs, previous_fs; //fieldsets
			var left, opacity, scale; //fieldset properties which we will animate
			var animating; //flag to prevent quick multi-click glitches

			$(".next").click(function(){
			if(animating) return false;
			animating = true;

			current_fs = $(this).parent();
			next_fs = $(this).parent().next();

			//activate next step on progressbar using the index of next_fs
			$("#progressbar2 li").eq($("fieldset").index(next_fs)).addClass("active");

			//show the next fieldset
			next_fs.show(); 
			//hide the current fieldset with style
			current_fs.animate({opacity: 0}, {
			step: function(now, mx) {
			//as the opacity of current_fs reduces to 0 - stored in "now"
			//1. scale current_fs down to 80%
			scale = 1 - (1 - now) * 0.2;
			//2. bring next_fs from the right(50%)
			left = (now * 50)+"%";
			//3. increase opacity of next_fs to 1 as it moves in
			opacity = 1 - now;
			current_fs.css({
			'transform': 'scale('+scale+')',
			'position': 'relative'
			});
			next_fs.css({'left': left, 'opacity': opacity});
			}, 
			duration: 800, 
			complete: function(){
			current_fs.hide();
			animating = false;
			}, 
			//this comes from the custom easing plugin
			easing: 'easeInOutBack'
			});
			});

			$(".previous").click(function(){
			if(animating) return false;
			animating = true;

			current_fs = $(this).parent();
			previous_fs = $(this).parent().prev();

			//de-activate current step on progressbar
			$("#progressbar2 li").eq($("fieldset").index(current_fs)).removeClass("active");

			//show the previous fieldset
			previous_fs.show(); 
			//hide the current fieldset with style
			current_fs.animate({opacity: 0}, {
			step: function(now, mx) {
			//as the opacity of current_fs reduces to 0 - stored in "now"
			//1. scale previous_fs from 80% to 100%
			scale = 0.8 + (1 - now) * 0.2;
			//2. take current_fs to the right(50%) - from 0%
			left = ((1-now) * 50)+"%";
			//3. increase opacity of previous_fs to 1 as it moves in
			opacity = 1 - now;
			current_fs.css({'left': left});
			previous_fs.css({'transform': 'scale('+scale+')', 'opacity': opacity});
			}, 
			duration: 800, 
			complete: function(){
			current_fs.hide();
			animating = false;
			}, 
			//this comes from the custom easing plugin
			easing: 'easeInOutBack'
			});
			});

			$(".submit").click(function(){
			return false;
			})

			setInterval(function(){document.getElementById("geradorId").className="activarAzul"}, 50);
			setTimeout(function(){
				
			if(window.innerWidth<780){
			document.getElementById("mySidebar").style.width = "0";
			document.getElementById("main").style.marginLeft= "0";
			document.getElementById("main").style.width="100%";
			document.getElementById("main").style.marginBottom="0";
			document.getElementByClassName("capitalize").style.class="pequeno";
			}
			}, 50);